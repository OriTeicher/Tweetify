// * ID'S
const GUEST_ID = 'GUEST'
const ID_LENGTH = 5

// * POSTS
const DEMO_POSTS_NUM = 10

// * LIKES
const LIKE = 1
const UNLIKE = -1
const LIKES_FIELD = 'likes'

export const constsService = {
    DEMO_POSTS_NUM,
    LIKE,
    UNLIKE,
    LIKES_FIELD,
    GUEST_ID,
    ID_LENGTH
}
